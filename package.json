{
  "name": "krashen",
  "version": "4.1.3",
  "description": "Browser-based comprehensible input tool for language learning",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "vitest": "^2.0.0"
  }
}
